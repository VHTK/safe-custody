spring:
  application:
    name: custody-backend
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848
        namespace: dev
        file-extension: yaml
        prefix: custody-backend-application
        group: DEFAULT_GROUP
      discovery:
        server-addr: 127.0.0.1:8848
        service: ${spring.application.name}
        namespace: dev

